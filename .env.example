# Copy this file to ".env" and fill in your values. NEVER commit the real .env.
# Every lesson runs without these (MOCK mode); add them for real output.

# Claude API key — get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Snowflake (used from Module 4 onward) — leave blank until then
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_PASSWORD=
SNOWFLAKE_WAREHOUSE=
SNOWFLAKE_DATABASE=
SNOWFLAKE_SCHEMA=
